name: 'Corne White'
on:
  workflow_dispatch:
  push:
    paths:
      - '*build.yaml'
      - 'includes/*'
      - 'config/corne_white*'

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix_path: corne_white_build.yaml            # default: "build.yaml"
      archive_name: corne_white                            # default: "firmware"

  drawer:
    needs: build
    uses: caksoylar/keymap-drawer/.github/workflows/draw-zmk.yml@main
    permissions:
      contents: write
    with:
      keymap_patterns: "config/corne_white.keymap"         # path to the keymaps to parse
      draw_args: "corne:'-k corne_rotated -l LAYOUT_split_3x5_3'"
      config_path: "config/corne_white.yaml"               # config file, ignored if not exists
      output_folder: "images"                              # path to save produced SVG and keymap YAML files
